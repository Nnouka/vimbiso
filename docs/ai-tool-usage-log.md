# Vimbiso — AI Tool Usage Log

*A running record of how AI tools were used to build this project, kept from ideation through the 10-day build. This is the source material for the hackathon's required written summary (SUB-3) and for judging on "AI Coding Usage" — update it as the build proceeds so the eventual summary is a report of what actually happened, not a reconstruction.*

The running principle across every phase below: **AI drafts, a human verifies before anything ships**, especially anywhere safety, trust, or accuracy is at stake. That standard was set early (translation review) and then applied recursively to research claims, product mechanisms, and even the country/market selection itself.

---

## Phase 1 — Competitive ideation (multiple independent AI agents, web-grounded)

Rather than developing one idea top-down, four independent AI agents were each briefed with the hackathon rubric and assigned a track, then sent to research the web and produce a fully-sourced venture proposal and pitch, competing against each other:

- **Amara Okafor** (Transparency & Accountability, Nigeria) — a WhatsApp/USSD budget-project tracker, grounded in real BudgIT/ICPC/GSMA data.
- **Tendai Moyo** (Safety, Reporting & Protection) — became Vimbiso, the eventual winner.
- **Fatou Diallo** (Stability & Social Cohesion, Sahel) — a voice/USSD mediation-routing line for farmer-herder conflict.
- **Youssef El-Amin** (cross-track, Tunisia) — a refugee rights/safety/rumor-correction tool.

Each agent was required to cite real sources for every claim rather than invent statistics, and each was judged against the brief's four equally-weighted criteria (Uniqueness, Scalability, AI Coding Usage, Presentation). Vimbiso won on the strength of its cryptographic matching-escrow mechanism (adapted from Stanford's Callisto project) and its disciplined AI-coding story.

**AI's role:** generating and researching multiple competing directions in parallel, at a speed no single founder could match, so the selection was made from real, sourced options rather than the first idea that came to mind.
**Human/oversight role:** the judge (Nnouka) selected the winner and set the evaluation criteria.

---

## Phase 2 — Critical stress-testing, using real-world evidence to correct a safety-critical design flaw

The winning design's original safety mechanism — a survivor's report stays sealed unless a second, independent survivor names the same perpetrator — was challenged directly: does this mean a lone survivor is left unprotected? AI web search was used to verify this wasn't hypothetical: real, current reporting (Human Rights Watch, Aug 2026; Cameroon's Ministry of Women's Empowerment; multiple local outlets) documented an active, escalating femicide crisis in Cameroon, overwhelmingly involving intimate partners — a pattern the matching-escrow design structurally cannot help, since intimate partner violence has one victim per perpetrator, not multiple victims to eventually "match."

This evidence was put back to the agent (in the Tendai Moyo persona) as a direct challenge, not accepted as a flaw to defend. The agent's response redesigned the core mechanism: every report now runs through mandatory risk-triage first (based on validated real-world tools — Johns Hopkins' Danger Assessment, the UK's DASH checklist, Maryland's Lethality Assessment Program), with an automatic high-risk response that fires from a single, solo report. The matching-escrow feature was kept but demoted to a secondary, non-urgent signal for a different, better-suited threat model (serial institutional predators, not intimate partners).

**AI's role:** surfacing the disconfirming evidence via web search, and then doing the actual mechanism redesign once challenged.
**Human/oversight role:** asking the question that exposed the flaw in the first place, and requiring the agent to fix the design rather than defend it — the single most consequential correction in the project's development.

---

## Phase 3 — Comparative, evidence-based market/geography selection

The pilot country (initially Zimbabwe, an artifact of how the founder persona was first set up rather than a deliberate choice) was challenged and re-decided through a structured comparison across five countries (Zimbabwe, Kenya, Cameroon, Rwanda, Tanzania) on crisis severity/currency, existing institutional infrastructure to route survivors into, language fit, mobile/USSD penetration, and civic-tech heritage — using fresh web research for each. Kenya was selected on the merits (current, multi-sourced femicide data; a real national helpline, HAK/1195; Ushahidi's own Kenyan origin as the direct inspiration for the corroboration model), and this was independently confirmed by checking the actual Andela hackathon community's participating country chapters.

**AI's role:** running the multi-factor comparison and web research across five candidate countries and defending a specific, ranked recommendation rather than an "any could work" hedge.
**Human/oversight role:** naming the omission (why Zimbabwe and not X?) that triggered the re-analysis, and supplying the real hackathon community data that sharpened the final call.

---

## Phase 4 — Re-planning around real team facts

When the actual team make-up was disclosed — Nnouka, Cameroonian, based at CMU Africa in Kigali, fluent in English/French/Pidgin, available to review French translation personally — the multilingual plan and the country narrative were both reworked to reflect it honestly: French moved from a speculative "hope a volunteer answers" slot to a guaranteed FULL-tier language with a named, real reviewer; the earlier decision to keep Kenya (not Cameroon) as the pilot was re-examined on its merits rather than assumed to flip just because the founder has personal ties to Cameroon, and held — for a stated institutional-readiness reason, not inertia. Cameroon and Rwanda were instead written into the plan as named, honest next-expansion markets.

**AI's role:** re-deriving the language-tier priority order and demo plan under the new facts, and explicitly re-litigating (rather than reflexively reversing) the Cameroon-as-pilot question on its actual merits.
**Human/oversight role:** supplying the real constraint (an actual bilingual reviewer, actual lived ties to two relevant markets) that the plan had to be honestly re-priced against.

---

## Phase 5 — Structured decomposition into an agent-buildable backlog

The finalized product and MVP scope were decomposed into a full Epics → Sprints → User Stories backlog (`vimbiso-backlog.md`), written at a level of detail intended for a coding agent (e.g. Claude Code) to pick up a single story and implement it correctly without further clarification: every story carries Given/When/Then acceptance criteria, exact technical notes (data model fields, endpoints, libraries), explicit dependencies, and a definition of done. The safety-critical content (the 8 triage questions and their override-scoring rules) was written once as a reference table other stories point to, rather than re-derived per story — reducing the chance an implementing agent invents its own version of safety-critical logic.

**AI's role:** the actual decomposition — turning a product spec into buildable, testable units of work.
**Human/oversight role:** requesting the level of detail needed specifically because the work would be handed to agents, and reviewing the resulting backlog before build start.

---

## Phase 6 — The build itself (log entries added as the 10 days progress)

This section is a template — append one entry per significant AI-assisted build session, so the written summary can quote specifics rather than assert "we used AI to build this" in the abstract.

**Where AI pair-programming is expected to matter most, per the backlog's own technical notes:**
- Test-driven development of the risk-scoring function (TRI-2) and the perpetrator-hashing utility (SEC-1) — both are safety/privacy-critical, both are pure functions well-suited to writing the test first, then generating and verifying the implementation against it.
- Scaffolding the WhatsApp bot's conversation state machine (INF-1/INF-3) and the Twilio SMS alerting integration (HR-2).
- Drafting first-pass translations (Swahili, French, and any stretch languages) for human review — never shipped unreviewed for safety-critical strings.
- Building the counsellor dashboard (DASH epic) and the Pattern Watch hash-matching logic (PW epic).
- Generating the seed data ingestion for the Kenyan resource directory (DIR-1), with every entry still manually source-checked against its cited URL before use.

**Log format (copy this row per entry):**

| Date | Story ID(s) | What AI did | What a human verified/changed | Notes |
|---|---|---|---|---|
| 2026-09-11 | Sprint 1 kickoff (all) | Six role-specialized AI agents (PM, Designer, QA, Networking, Backend, Frontend) worked from the locked backlog: PM wrote the interface contracts and file-ownership map before anyone touched code; Designer wrote the full WhatsApp conversation script; QA wrote failing Jest tests for `scoreRisk`/`normalizeAndHash` *before* those functions existed (TDD); Networking built the Twilio WhatsApp/SMS integration layer against a frozen function-signature contract; Frontend scaffolded a standalone dashboard app ahead of its Sprint 2 stories; Backend implemented the schema, hashing, scoring, content pipeline, and conversation state machine against QA's tests and Designer's copy. | A human (Nnouka) reviewed and approved the role split and the "coordinate first, then build" sequencing before work started. Backend could not run `npm install` or real `npx jest` in the build environment (npm registry blocked at the network-policy level, confirmed via direct `curl` test, not just proxy config) — verified logic instead by running QA's actual, unedited test assertions through a hand-rolled Jest-compatible shim (18/18 riskScoring cases, 14/14 hashing cases passed) and by `node --check` syntax-validation on every file. Real `npm install && npm test` on a machine with normal registry access is still required as the authoritative check before Sprint 1 is signed off. | Two interface deviations were surfaced and explicitly flagged rather than silently absorbed: `sendList()` needed a 4th optional parameter beyond the originally frozen contract (additive, non-breaking); an env var naming mismatch between the backlog's shorthand and Networking's actual code was caught and resolved by standardizing on one name. Both are the kind of integration friction real teams hit, surfaced by agents holding each other to the written contract instead of silently reconciling differences. |
| 2026-09-11 | TS conversion + team workflow (all) | The entire Sprint 1 codebase (`server/**/*.js` → `.ts`, `dashboard/**/*.js` → `.ts`) was converted to TypeScript strict mode by an AI agent, file by file, preserving behavior exactly except where the type checker surfaced a real latent bug. Separately, a PM-role agent authored the team's git workflow (`CONTRIBUTING.md`: branch naming, PR requirements, a per-area reviewer-pairing table, what a reviewer actually checks), `.github/CODEOWNERS`, a PR template, and the root `README.md` covering full local setup for both apps. | A human (Nnouka) requested both the TypeScript conversion and the workflow/README in the same turn and reviewed the resulting docs. TypeScript's strict-mode type checker (run via manual reading + `node --check`, since `npm install` was already blocked by this point — real `tsc` wasn't available yet in this environment) caught two genuine latent bugs during the conversion: a corrupted `conversation_state.current_step` silently reset a survivor to question 1 with no error logged (fixed with a proper type guard); a duplicated `X-Twilio-Signature` header (`string[]` instead of `string`) would have flowed unguarded into Twilio's signature validator (fixed with an explicit array check). | This is the clearest example in the project so far of TypeScript's type system doing real safety-relevant work, not just style enforcement — both bugs were in code paths a survivor's session or a webhook's authenticity depends on. `CODEOWNERS` ships with placeholder GitHub handles until the real team's usernames are known; it does nothing until then. |
| 2026-09-12 | Sprint 2 (all) | Five role-specialized AI agents worked Sprint 2 in two TDD waves per a PM-authored `docs/sprint-2-plan.md` (frozen interface contracts, content-key list, ownership map): Wave 1 — Designer wrote all HR/DIR/PW copy plus draft Swahili/French translations of the safety-critical subset (explicitly `tier='PARTIAL'`, unreviewed); QA wrote 13 failing test cases for `patternMatch.ts` (PW-2) against a function that didn't exist yet, plus 4 Playwright specs for the dashboard and a `qa-sprint2-checklist.md`. Wave 2 — Backend implemented HR-1..4/DIR-1..3/PW-1..3 against QA's tests and Designer's copy (without editing QA's test file), plus 5 new seed files; Networking added the SMS-alert helper with an explicit "PII-free body" contract comment; Frontend rebuilt the dashboard's 4 stories against real Postgres queries. | A human (Nnouka) requested "begin next sprint" and the PM (this session) resolved 4 ambiguities QA flagged in the frozen contract before Backend implemented against them (documented in `patternMatch.ts`'s own header, not just this log). The PM then independently re-verified the work rather than accepting each agent's self-report: re-ran all 13 of QA's `patternMatch.test.ts` cases from scratch against Backend's implementation (13/13 pass), and — new this sprint — discovered this sandbox has a global TypeScript compiler available even though `npm install` is still blocked, and used it to run a real `tsc --noEmit`-equivalent check across every `.ts` file in the repo (via a hand-built ambient-types shim standing in for the uninstallable `@types/*` packages). That check caught one genuine bug: `content_en.ts` defined `menu.body` twice (a stale Sprint 1 value alongside Sprint 2's 4-option replacement) — a duplicate-object-key error TypeScript's strict mode refuses to compile. Fixed by removing the stale line. | This sandbox's "no `npm install`" limitation turned out to be narrower than assumed: a global `tsc` binary was already present, which meant a real compiler check — not just careful reading — was possible for the first time this build, and it paid for itself immediately by catching a bug no amount of manual review had caught across two agents' worth of edits to the same file. Worth carrying forward as standard practice for the rest of this build, and worth a line in the written summary as a concrete example of AI-tool-usage self-correction, not just AI-tool-usage output. |
| 2026-09-12 | QA-2, QA-3, QA-4 (Sprint 3) | Discovered a second sandbox capability beyond Sprint 2's `tsc`: PostgreSQL 16 is pre-installed here. Built real (not type-only) runtime shims for the four npm packages this sandbox can't install (`pg`, `twilio`, `dotenv`, `bcrypt`) — critically, the `pg` shim routes `Pool.query()` through the real `psql` CLI against a real local database instead of returning canned data — then ran the project's ACTUAL, unmodified files through them via `tsx`: `server/migrations/run.ts` (all 11 tables created), `server/seeds/run_all.ts` + `dashboard/seed.ts` (real seed data), and a hand-written end-to-end driver calling `handleIncomingMessage()` directly (the same function a Twilio webhook calls) across 5 full conversation journeys — English HIGH-risk with a trusted contact and a Pattern Watch match, Swahili STANDARD-risk with a region picker, Swahili HIGH-risk (closing a gap HR-1's own DoD calls out by name), English menu-browsing, and an isolated DIR-2 "Other/National" fallback check. Every result was then verified with direct `psql` queries against the resulting database, including a purpose-built audit query searching every text/jsonb column of every table for a raw perpetrator identifier (found in zero places outside its intended hashed form) and the dashboard's own exact SQL query strings run standalone to check DASH-2/3/4 without needing a real `express`/`ejs` install. | A human (Nnouka) requested "sprint 3." The PM (this session) treated the shim itself as untrusted until proven — the same discipline as Sprint 2's `tsc` shim — and found and fixed 5 real bugs in the harness before trusting any of its output as evidence: (1) `psql -t` suppresses column headers, which silently broke the TSV parser and made a real match look like a non-match; (2) removing `-t` reintroduced Postgres's own command-completion tags (e.g. "INSERT 0 1") as bogus extra data rows, fixed by adding `-q`; (3) TIMESTAMP columns came back as plain strings rather than JS `Date` objects, breaking `conversation.ts`'s own `.getTime()` call — fixed by adding timestamp-to-Date coercion; (4)/(5) a jsonb column and a content template string that both happen to look like `{...}` were initially confused with a Postgres array literal and with each other, fixed by strictly ordering the coercion checks (integer-array regex first, then JSON.parse only for values that parse to an object/array, everything else stays a string). Two of the five real conversation journeys were run twice in the course of debugging the harness, producing two duplicate (but internally consistent) report rows for one test number — noted here explicitly so it isn't mistaken for a product bug when read later. | The general lesson carried over from Sprint 2's `tsc` discovery holds again, one level up: a hand-built test harness is itself code, and needs the same "don't trust it until it's independently verified" discipline as the application code it's testing — a shim bug produces exactly the same symptom as a real one, and only root-causing each one separately (rather than assuming either "the harness must be right" or "the app must be buggy") kept this sprint's evidence trustworthy. The specific bugs (TSV header suppression, command-completion tags, jsonb/array/string coercion ordering) are worth keeping as a checklist for anyone building a similar `psql`-backed `pg` shim in a future sandboxed-npm environment. Full evidence in `docs/qa-script.md` and `docs/qa-sprint3-report.md`. |
| 2026-09-14 | INF-1/INF-3 (crash found via live Twilio testing) | Nnouka's first real message on the live Twilio Sandbox crashed the webhook handler with two symptoms: `content.ts` logging "missing content" for every `lang_select.*` key, then `sendList` throwing "row title ... is 25 chars." Root-caused rather than patched blind: `server/index.ts` auto-runs migrations on boot but NOT seeding (a separate manual `npm run seed` step), so a fresh environment can boot with an empty-but-schema-complete `content_strings` table and no startup error at all — the bare-key fallback that's supposed to make a missing key "loud but harmless" turned out to be neither, because the key string itself ("lang_select.row_kiswahili", 25 chars) is longer than WhatsApp's own 20/24-char title limits, so the very first screen of every conversation hard-crashed instead of degrading. Fixed content.ts's fallback to truncate to the tightest real WhatsApp UI-chrome limit (20 chars) and added a loud, specific `assertCriticalContentSeeded()` startup check. | The PM (this session) reproduced the exact bug for real before trusting the fix: created a fresh Postgres database, ran ONLY the real migration runner against it (no seed), confirmed `content_strings` had 0 rows, then ran the actual `content.ts`/`whatsapp.ts` code against it and watched `sendList` throw with the identical error text Nnouka reported. Verified the fix the same way — same empty database, same driver, confirmed the new startup check fires with the exact missing keys named and `sendList` now succeeds — then ran the real seed script and re-ran the driver a third time to confirm the fully-seeded path produces zero warnings and the real translated text. | This is the sharpest example yet of a fix being trusted only after reproducing the reported failure verbatim against real infrastructure, not after "this should fix it" reasoning from reading the stack trace alone — the stack trace alone would have pointed at `sendList`'s length validation as the bug, when the real defect was one layer up in what `content.ts` hands it under a specific, previously-untested startup-ordering condition. |
| 2026-09-15 | LOG-1, LOG-2, LANG-8 (new backlog stories, not yet built) | Nnouka asked for three new capabilities to be added as user stories: a replayable message log for data analysis with anonymized sender ids, a dev-only console mirror of it, and tasteful emoji in WhatsApp copy for engagement. Rather than writing the ACs from the request's surface wording alone, the PM (this session) did real design work first: reused SEC-1's existing HMAC-SHA256 hashing pattern for sender anonymization but specified a SEPARATE secret from `PERPETRATOR_HASH_SECRET` (so the two hashed-identifier spaces can't be correlated if one secret ever leaks) rather than reusing it; specified AES-256-GCM application-level encryption for message body content at rest (not just relying on infra-level disk encryption), reasoning explicitly that survivor free-text is exactly the class of data this project's own SEC epic already refuses to store raw elsewhere; and scoped the emoji story with an explicit exclusion list (`highrisk.*`, `pw.*`, `disclosure_message`) so warmth never bleeds into safety-critical copy. | Nnouka's request was specific about outcomes (replayable, anonymous sender ids, encrypt what needs encrypting "if you research and feel there will be any privacy concerns," dev-only console, appropriate emoji) but left the how to the PM's judgment — exactly the kind of ambiguity this backlog's existing stories are written to resolve explicitly rather than silently. The PM flagged one open question rather than deciding it unilaterally: no retention period is specified for the new log, and indefinite storage of even anonymized/encrypted survivor data is a real tradeoff worth a human decision, not a default. | These two stories are backlog-only per Nnouka's literal request ("add this to the user stories") — no code, schema, or migration was written for LOG-1/LOG-2/LANG-8 in this pass; `docs/backlog.md`'s Section 2 data model reference gained a proposed (not yet migrated) `message_log` schema so the design is concrete enough to implement directly whenever this is picked up. |
| 2026-09-15 | LOG-3 (new backlog story, not yet built) — follow-up to the row above | Nnouka immediately followed up, mid-turn, with a real requirement the first pass hadn't handled: sender ids must be recoverable if privacy protections are properly lifted (a lawsuit, a government request), while still supporting "same conversation / same sender" grouping without identification day-to-day. The PM (this session) recognized this could NOT be satisfied by simply keeping the existing one-way HMAC design — a cryptographic hash is irreversible by construction, with or without the key, so "anonymous but recoverable" needs a genuinely different mechanism, not a tweak to the first one. Redesigned as a two-tier system: `message_log.sender_pseudonym` stays a true one-way hash (used for all grouping/analysis, never reversible by anyone), while a NEW, separate `sender_identity_map` table holds the real number under REVERSIBLE AES-256-GCM encryption, keyed by a THIRD secret deliberately kept out of the app's normal runtime configuration and held by a designated custodian — plus an `identity_recovery_log` so every actual re-identification is auditable, never silent. Explicitly did not build a decrypt-on-demand API, reasoning in the story itself that doing so would defeat the entire point of keeping the key out-of-band. | Nnouka additionally asked for this to "figure in the user stories and spec and presentations," not stay backlog-only. The PM propagated the same design across every document a judge or teammate might actually read: `docs/backlog.md` (the new LOG-3 story plus a revision note on LOG-1 marking its earlier "anonymized" language as superseded by an explicit irreversible/reversible distinction), `docs/data-model.md` (synced schema, per that file's own "backlog.md is the source of truth, keep this file in sync" rule), `docs/mvp-spec.md` (a one-line roadmap addition), `docs/written-summary.md` (a new paragraph framing this as a deliberate trust tradeoff, not a feature checkbox — plus an entry in the "still simulated" honesty section so it's never overstated as built), and the pitch deck's existing "Trust & privacy by design" slide (`submission/vimbiso-pitch-deck.pptx`/`.pdf`, rebuilt and visually re-verified via a rendered PNG of the actual slide). | The deck edit is the one place this required real judgment beyond writing text: the new privacy point uses a visibly different badge color/glyph (a muted gray "→" instead of the other three points' green "✓", confirmed by rendering the slide as an image) specifically so a design meant to communicate "not yet built" couldn't accidentally visually imply "verified," matching this project's existing standard of never rounding up. Same standard applied to flagging real open questions rather than deciding them: who the key custodian actually is, what counts as a valid lawful request, and where the recovery key is physically stored are all named as human/legal decisions in the story, not assumed. |
| 2026-09-14 | INF-1/INF-3 (stuck-at-language-select, found via live Twilio testing) | Nnouka shared a real WhatsApp screenshot showing a survivor tapping "English" from the language-selector list and getting the exact same selector re-sent, repeatedly. The PM (this session) traced this to a risk this codebase had already flagged on itself: `webhook.ts`'s header comment named its `ButtonPayload`-carries-list-taps assumption "NOT VERIFIED against a live payload... the single highest-risk assumption in this file," written back when no Twilio account existed to check it against. Rather than pick one alternative field name and hope, used `WebSearch`/`WebFetch` against Twilio's own public docs to ground the fix in real, current API behavior (Twilio's messaging-webhook-request reference confirms `ButtonPayload` for quick-reply buttons but explicitly punts list-picker specifics to the `InteractiveData` JSON blob) and cross-referenced that against WhatsApp's own Cloud API shape for a list-row reply (`list_reply: {id, title}`, distinct from `button_reply`). Implemented `extractButtonId()`: tries `ButtonPayload` first (quick-reply buttons unchanged), falls back to parsing `InteractiveData` for `list_reply.id`/`button_reply.id`, never throws on malformed JSON. Added a diagnostic log line (deliberately excluding the survivor's free-text `Body`) so the very next real tap either confirms this was right or shows exactly what Twilio actually sent. | Nnouka staged the actual screenshot from their device into this session so the PM could see the real WhatsApp thread rather than working from a text description alone. The PM then built a minimal test-only `express` shim (this sandbox has no real `express` package) specifically so the REAL, unmodified `server/routes/webhook.ts` router could be loaded and its actual POST handler invoked directly — not a reimplementation of its logic — with synthetic requests shaped like a quick-reply tap, a list-picker tap, an unrecognized tap, and malformed JSON, confirming each resolved (or safely failed to resolve) a button id correctly. Went one step further than isolated unit verification: ran the real handler through `loadContentCache()` and a real Postgres `conversation_state` row for a synthetic survivor number shaped exactly like the hypothesized Twilio payload, and confirmed the row actually advanced from `AWAITING_LANGUAGE` to `MAIN_MENU` with `language='en'` — reproducing the reported bug's exact symptom against a real database, then confirming the fix closes it. | Explicitly flagged to Nnouka as evidence-based but not yet payload-confirmed: this sandbox cannot reach a live Twilio Sandbox to capture Nnouka's actual raw webhook body, so the fix is grounded in Twilio's documented behavior plus WhatsApp's own API shape, not a direct capture of the failing request. The new diagnostic log line exists specifically to close that gap on the very next live test, honestly narrowing (not eliminating) the sandbox's fundamental inability to reach real Twilio infrastructure. |
| 2026-09-14 | HR-5 (post-Sprint-3, found via live Twilio testing) | Nnouka's live Twilio Sandbox testing surfaced a real bug reported verbatim as "counselor cannot add their phone number." The PM (this session) traced it via `grep` to a genuinely orphaned schema column (`counsellor_users.phone_number_for_sms`, present since Sprint 1's migration but never read or written by any code path — HR-2's alert was hardcoded to a single `ONCALL_COUNSELLOR_PHONE` env var). Built the full fix: migration `011_add_counsellor_on_call.sql`; a new `alertOnCallCounsellors()` (plural) in `sms.ts`, added alongside — not replacing — the existing frozen `alertOnCallCounsellor()` singular per `sprint-2-plan.md §3.2`'s frozen-contract rule; `conversation.ts`'s `handleConnectResponse` rewired to call it and write one `sms_alerts` row per recipient; a new `/dashboard/settings` page (GET/POST routes, `settings.ejs`, nav link) letting a counsellor set their own number and toggle on-call status, reusing `conversation.ts`'s existing phone-format regex for consistency. | Nnouka reported the bug from real testing and, when asked "build it now vs. quick fix vs. defer," chose the full build. The PM then re-ran Sprint 3's exact real-database methodology rather than treating this as done on inspection alone: restarted the same real Postgres instance, applied the new migration through the project's REAL, unmodified `server/migrations/run.ts` (confirmed via `\d counsellor_users`), then ran the REAL `alertOnCallCounsellors()` directly against real data across 4 scenarios (fallback-when-nobody-configured, single on-call counsellor, two on-call counsellors alerted independently, a NULL-number counsellor correctly excluded), then drove the REAL state machine end-to-end via `handleIncomingMessage()` for two full HIGH-risk journeys (one with a configured counsellor, one falling back to the env var) and confirmed via live query that `conversation.ts`'s rewired call site wrote the correct real `sms_alerts` rows in each case — not just that the isolated `sms.ts` function behaved correctly. The dashboard settings route's own query/validation logic was separately verified against the same real database via `dashboard/lib/db.ts`. | Honest limitation carried forward unchanged from Sprint 2/3: no real `express`/`ejs` package exists in this sandbox, so the actual HTTP request/response cycle through the new `/dashboard/settings` page has still never been driven end-to-end here — only its underlying query and validation logic. All test-only database rows created during verification were cleaned up afterward; the seeded demo counsellor was restored to its original blank/not-on-call state. |
| 2026-09-15 | LOG-1, LOG-2, LOG-3, LANG-8 (built and real-DB-verified — follow-up to the two backlog-only rows above) | Nnouka asked "did you implement them?" then, on hearing no, said "yes build all, employ devs if needed." The PM (this session) implemented the full two-tier design from the prior rows for real: migration `012_create_message_log.sql` (`message_log`, `sender_identity_map`, `identity_recovery_log`); `server/lib/messageLog.ts` (`pseudonymizeSender()` HMAC-SHA256 under `MESSAGE_LOG_HASH_SECRET`; `encryptMessageBody()`/`decryptMessageBody()` and `encryptSenderIdentity()` AES-256-GCM under `MESSAGE_LOG_ENCRYPTION_KEY`/`SENDER_IDENTITY_RECOVERY_KEY` respectively; `consoleMirror()` for LOG-2; the fire-and-forget, internally try/caught `logMessage()` entry point); wired at every real send/receive chokepoint (`webhook.ts` inbound, `whatsapp.ts`'s `sendText`/`sendButtons`/`sendList` and `sms.ts`'s `sendSms` outbound); `server/scripts/recoverSenderIdentity.ts`, a standalone CLI (never imported by the running app) implementing LOG-3's out-of-band recovery procedure; three new keys documented in `.env.example`, with `SENDER_IDENTITY_RECOVERY_KEY`'s comment explicit that it must never live in normal app config; and LANG-8's emoji pass on `content_en.ts`'s explicitly-approved low-stakes keys only. | Nnouka's instruction to "employ devs if needed" was available but the PM judged direct implementation more reliable for safety/privacy-critical cryptographic code than delegating to sub-agents, consistent with how the rest of this session's "role" work was actually done. Verification followed this project's established real-database methodology, one level deeper than before: a full `tsc -p tsconfig.json` run across the whole repo showed zero new errors from any of the new/modified files; a real HIGH-risk survivor journey was driven end-to-end through the REAL, unmodified `webhook.ts`/`conversation.ts`/`whatsapp.ts`/`sms.ts` against a real Postgres database, confirming correct `message_type` resolution for text/button/list taps, stable pseudonym grouping across inbound AND outbound rows, exactly one `sender_identity_map` row per unique sender despite 12+ messages, exact plaintext round-trip via `decryptMessageBody()` for both a survivor's message and an HR-2 SMS body, and live `NODE_ENV` toggling proving the console mirror is verbose in dev and silent in production; `recoverSenderIdentity.ts` was then run three ways against that real data — a successful recovery (correct real number decrypted, a real `identity_recovery_log` row written), a missing-key rejection, and an unknown-pseudonym rejection (neither of the latter two writing a phantom audit row); LANG-8's emoji-bearing values were checked programmatically against WhatsApp's real 24/20-character title limits using the same UTF-16 `.length` semantics `whatsapp.ts` itself validates against, since INF-1's earlier crash had already proven that distinction matters. A real bug was found and fixed IN THE TEST HARNESS, not the app: the scratch `pg` shim's SQL-literal `quote()` function had no case for `Buffer` values (BYTEA columns were new to this schema), stringifying raw ciphertext bytes and corrupting the round-trip — fixed by adding a proper hex-escape (`E'\\x<hex>'::bytea`) encode/decode path, documented inline in the shim itself as scratch-only. | This is the first story in this project where the AI-usage log records catching a bug in its OWN verification tooling for a completely new column type (BYTEA) rather than in the seed/migration logic being tested — worth keeping as a checklist item (Buffer/BYTEA handling) for any future `psql`-backed `pg` shim in a sandboxed-npm environment, alongside Sprint 3's TSV/jsonb/array coercion lessons. All new-secret env vars use this project's `openssl rand -hex 32` convention; the retention-period and real-world key-custody-policy questions LOG-1/LOG-3 flagged remain genuinely open, not resolved by this build pass. |
| 2026-09-15 | HR-2 (SMS alert wording, real usability feedback) | Nnouka pasted the actual SMS body a counsellor would receive (`Vimbiso alert: report #6, risk=HIGH, 2026-09-14T12:09:27.551Z`) and pointed out, correctly, that it reads like a system log line — not something that would make a real person act urgently — and asked for wording that says what HIGH actually means in plain language. The PM (this session) rewrote it as `Vimbiso URGENT: report #6 - someone on our safety line may be in immediate danger and asked for a counsellor. Respond now. (2026-09-14T12:09:27Z)`, keeping the SAFETY-CRITICAL constraint from HR-2's own AC unchanged (report_id/risk_level/timestamp ONLY, still no survivor name/number). Pulled the message-building logic (previously duplicated inline in both `alertOnCallCounsellor()` and `alertOnCallCounsellors()`) into one shared `buildAlertBody()` specifically so the two call sites can never drift apart on wording again — a small refactor volunteered rather than requested, since the duplication was directly in the way of the fix. Also caught, unprompted, a real SMS-delivery cost/reliability detail: using an em dash instead of a plain hyphen would have forced the whole message out of the GSM-7 character set into UCS-2 encoding, silently cutting the single-segment limit from 160 characters to 70 and doubling the real segment count/cost — kept the rewrite pure ASCII specifically to avoid that. | Nnouka's feedback was itself the verification signal — a real person saying a real message doesn't communicate urgency is exactly the kind of judgment call this project treats as ground truth, not something to second-guess. The PM then verified the REWRITE, not just the reasoning: ran both real functions through the same real Twilio-capture shim/database this project's other SMS verification uses, and confirmed programmatically (not by eye) that the new body is single-segment (145 chars), pure GSM-7-safe ASCII, still contains zero PII, and that both call sites produce byte-identical wording for the same inputs. | The STANDARD-risk wording branch in `buildAlertBody()` is defensive correctness, not a currently-exercised path — `conversation.ts`'s only real call site always passes `'HIGH'` literally, since this SMS alert only ever fires from HR-1's high-risk connect prompt. Worth a line in demo notes: the on-call counsellor now sees a message that names the actual stakes, not just a risk-level enum value. |
| 2026-09-15 | INF-1/INF-3 (third stuck-at-language-select report, found via live Twilio testing — after LOG-1's console logging shipped) | Nnouka pasted real console output showing a survivor still stuck at the language selector after the previous fix, but this time LOG-1's own new `[message_log]` lines made the actual payload visible without needing a new diagnostic pass: the inbound row showed `type=text buttonId=null body="lang_en"`, and the raw webhook diagnostic line showed every interactive field (`ButtonPayload`/`InteractiveData`) genuinely undefined. The PM (this session) recognized this as a DIFFERENT bug from the prior fix, not a failure of it: `extractButtonId()` was correctly finding nothing, because Twilio's Sandbox itself appears not to deliver this app's `twilio/list-picker` messages as genuinely interactive — the survivor's "tap" comes back as an ordinary text message whose body is the row's own internal id. Rather than chase Sandbox internals unreachable from this environment, fixed it where it's actually testable: collected every literal button/list id this file ever sends into one `KNOWN_INTERACTIVE_IDS` set and promoted a matching plain-text `Body` to a real `buttonId` at the top of `handleIncomingMessage()`, before any step handler runs — so a real tap and a degraded-to-text one are handled identically everywhere, with zero risk of misreading genuine survivor free text (the ids are code-only snake_case tokens). | Nnouka reported the bug by pasting real console output directly, which was itself evidence LOG-1's logging work from earlier the same day was already paying for itself as a debugging tool. The PM reproduced the EXACT reported payload shape (`Body: "lang_en"`, no interactive fields at all) against the real, unmodified webhook/conversation code and a real database, confirmed the bug reproduced (state stuck at `AWAITING_LANGUAGE`), then confirmed the fix closes it (state correctly advances to `MAIN_MENU`/`language=en`) — plus two explicit regression checks: a genuine `ButtonPayload` tap still works unchanged, and real unrelated free text ("I want English please") still correctly re-prompts rather than being swallowed by the new fallback. Full `tsc` typecheck showed zero new errors. | This is the same file's THIRD real bug found via live Twilio testing in as many days, each one layer deeper than the last (a missing-seed crash, then a wrong-field-checked extraction bug, then this — a channel-level behavior no documentation search could have predicted). The honest open question flagged in `backlog.md`: WHY the Sandbox degrades this specific content type to plain text is still not confirmed against Twilio's own documentation of that failure mode (unreachable from this build environment) — the fix is deliberately defensive (doesn't require knowing the exact mechanism) rather than a confirmed root-cause explanation. |
| 2026-09-15 | INF-1/INF-3 (numbered-option typed-digit support, proactive UX gap flagged by Nnouka) | Nnouka pointed out a real usability gap, not a bug report: "not all users may understand that they need to click and select, they may actually just send the number representing the option... the app should understand the option selected too," with the main menu as a worked example (typing "1" for "Report something"). The PM (this session) generalized this beyond the one example given, per the request's clear implication, to every option-presenting step in the state machine: a new `NUMBERED_OPTIONS_BY_STEP` map (plus a shared `TRIAGE_NUMBERED_OPTIONS` list for the 8 triage questions, which all send the same 3 buttons) gives the ordered option-id list each step last sent, and `handleIncomingMessage()` promotes a bare-digit `Body` to the buttonId at that 1-indexed position, scoped strictly to `state.current_step`. Deliberately excluded `AWAITING_TRUSTED_CONTACT_NUMBER` and `AWAITING_PW_IDENTIFIER` from the map, reasoning explicitly that both expect genuine free-typed text where a bare digit could be real survivor input (e.g. the start of a phone number) — silently reinterpreting it as a menu choice would corrupt or drop that input, a materially worse failure mode than leaving a rare miskeyed digit unrecognized. | Nnouka gave one worked example but the request's own wording ("when options are sent... the app should understand the option selected too") was clearly general, not scoped to the main menu alone — the PM treated this as the actual requirement rather than building only the literal example. Full `tsc` typecheck showed zero new errors from this change (three pre-existing, unrelated errors remain in `riskScoring.test.ts`/`migrations/run.ts`, confirmed unrelated by content and file). Verified against a real database via a new driver, `/tmp/vimbiso-e2e/numbered-options-check.ts`, 12 checks, all passing: the user's own main-menu example; a digit at the language selector; a digit reaching the trusted-contact step; digits mid-triage (checked against the actual `triage_answers` row written, not just `current_step`); a digit at the region picker; an out-of-range digit safely re-prompting; no regression to a real `ButtonPayload` tap or to the earlier `KNOWN_INTERACTIVE_IDS` text-id promotion; and, as the critical safety check, a bare digit at `AWAITING_TRUSTED_CONTACT_NUMBER` correctly staying on that step with the existing "not a complete number" re-prompt rather than being read as a menu pick. | This is the fourth real fix to this same state-machine entry point in four days (missing-seed crash, wrong-field extraction bug, Sandbox text-degradation bug, now this) — each has landed as an additive, narrowly-scoped promotion step ahead of the main step-dispatch chain rather than a rewrite of it, keeping the growing pile of real-world Sandbox/UX quirks legible instead of tangled into the dispatch logic itself. |

---

## Notes for the written summary (SUB-3)

When drafting the final submission text from this log, be specific and honest rather than general: name which parts were AI-drafted-then-human-reviewed (translations, especially) versus AI-and-human-collaboratively-designed (the risk-triage mechanism, the country selection) versus AI-generated-and-directly-used (research citations, the backlog itself, boilerplate code). The strongest, most credible claim available from this project's actual history is not "we used AI a lot" — it's "AI was used to generate and stress-test multiple real options fast, and every safety-critical or trust-critical output was still gated by explicit human verification," which is demonstrably true across every phase above.
