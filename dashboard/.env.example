# Copy to .env before running. Not yet consumed by server.js (DASH-1 wires
# real DB auth + a real session secret) — present now so the shape is settled.

PORT=3001
SESSION_SECRET=change-me-before-real-use

# DASH-1 will read counsellor_users through this connection.
# DATABASE_URL=postgres://user:password@localhost:5432/vimbiso
